# Node.js development environment
# Copy this file to your Node.js project root as .envrc
# Then run: direnv allow

use flake ~/.dotfiles/flakes/nodejs

# Optional: Load .env file if it exists
dotenv_if_exists

# Optional: Add project-specific environment variables
# export NODE_ENV="development"
# export PORT="3000"
# export DATABASE_URL="mongodb://localhost/myapp_dev"

# Optional: Add project-specific PATH entries
# PATH_add bin
# PATH_add node_modules/.bin